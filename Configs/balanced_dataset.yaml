# /content/balanced_dataset.yaml

# Root directory of the dataset (where images/ and labels/ subfolders live)
path: /content/yolo_balanced

# Relative paths to the image folders for each split
train: images/train
val: images/val
test: images/test   # <-- this is what evaluate.py uses to find test images

# Class names
names:
  0: non_debris
  1: debris